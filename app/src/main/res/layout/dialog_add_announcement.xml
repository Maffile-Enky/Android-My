<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/announcement_title_label"
        android:textSize="16sp"
        android:textColor="@color/counter_text"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etAnnouncementTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/announcement_title_hint"
        android:padding="12dp"
        android:background="@drawable/display_background"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/announcement_content_label"
        android:textSize="16sp"
        android:textColor="@color/counter_text"
        android:layout_marginBottom="8dp" />

    <EditText
        android:id="@+id/etAnnouncementDescription"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:hint="@string/announcement_content_hint"
        android:padding="12dp"
        android:background="@drawable/display_background"
        android:gravity="top|start" />

</LinearLayout>